name: Deploy → VPS

on:
  push:
    branches: [master]

jobs:
  # ──────────────────────────────────────────────────────────────────────────
  # Auditoria de segurança de dependências (não bloqueia deploy, mas notifica)
  # ──────────────────────────────────────────────────────────────────────────
  security-audit:
    name: Security Audit
    runs-on: ubuntu-latest
    continue-on-error: true   # não bloqueia deploy — apenas gera relatório
    steps:
      - uses: actions/checkout@v4.2.2

      - name: Setup Python 3.11
        uses: actions/setup-python@v5.3.0
        with:
          python-version: '3.11'

      - name: Python dependency audit (pip-audit)
        working-directory: backend
        run: |
          pip install pip-audit --quiet
          pip install . --quiet
          pip-audit --format=columns || true

      - name: Node.js dependency audit
        working-directory: frontend
        run: npm audit --audit-level=high || true

  # ──────────────────────────────────────────────────────────────────────────
  # Deploy principal
  # ──────────────────────────────────────────────────────────────────────────
  deploy:
    name: Deploy to production
    runs-on: ubuntu-latest
    timeout-minutes: 25
    environment: Hostinger
    needs: []   # deploy não depende do audit (paralelo)

    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          port: 22
          script: |
            set -euo pipefail
            cd /opt/senhas

            echo "==> [1/6] Backup do banco antes do deploy"
            BACKUP_FILE="/opt/senhas/backups/pre-deploy-$(date +%Y%m%d-%H%M%S).sql.gz"
            mkdir -p /opt/senhas/backups
            # Lê variáveis do .env — não dependem do ambiente do runner
            DB_USER=$(grep -E '^DB_USER=' /opt/senhas/.env | cut -d= -f2)
            DB_NAME=$(grep -E '^DB_NAME=' /opt/senhas/.env | cut -d= -f2)
            docker compose -f docker-compose.prod.yml exec -T postgres \
              pg_dump -U "$DB_USER" "$DB_NAME" \
              | gzip > "$BACKUP_FILE"
            echo "Backup salvo: $BACKUP_FILE"
            # Mantém apenas os 10 backups mais recentes
            ls -t /opt/senhas/backups/pre-deploy-*.sql.gz | tail -n +31 | xargs -r rm --

            echo "==> [2/6] Pull do código"
            git fetch --all
            git reset --hard origin/master

            echo "==> [3/6] Build das novas imagens (containers antigos ainda rodando)"
            docker compose -f docker-compose.prod.yml build --no-cache frontend backend

            echo "==> [4/6] Migrations"
            # Roda migration em container temporário enquanto prod ainda está up
            docker compose -f docker-compose.prod.yml run --rm --no-deps backend \
              alembic upgrade head

            echo "==> [5/6] Swap dos containers (downtime mínimo)"
            docker compose -f docker-compose.prod.yml up -d --no-deps --remove-orphans frontend backend

            echo "==> [5b] Reload do nginx (aplica mudanças de config sem downtime)"
            docker compose -f docker-compose.prod.yml exec -T nginx nginx -s reload || \
              docker compose -f docker-compose.prod.yml restart nginx

            echo "==> [DEBUG-TEMP] Diagnóstico de CSP (remover após investigação)"
            echo "--- host: nginx/conf.d/senhas.conf (linha CSP) ---"
            grep "add_header Content-Security-Policy" nginx/conf.d/senhas.conf || echo "NÃO ENCONTRADO no host"
            echo "--- container: arquivo montado em /etc/nginx/conf.d/senhas.conf ---"
            docker compose -f docker-compose.prod.yml exec -T nginx cat /etc/nginx/conf.d/senhas.conf | grep "add_header Content-Security-Policy" || echo "NÃO ENCONTRADO no container"
            echo "--- container: nginx -T (config efetivamente carregada) ---"
            docker compose -f docker-compose.prod.yml exec -T nginx nginx -T 2>/dev/null | grep "add_header Content-Security-Policy" || echo "NÃO ENCONTRADO via nginx -T"
            echo "--- container: qual arquivo compose está usando (docker inspect mounts) ---"
            docker inspect senhas-nginx --format '{{ range .Mounts }}{{ .Source }} -> {{ .Destination }}{{ "\n" }}{{ end }}'

            echo "==> [6/6] Health check pós-deploy"
            sleep 5
            for i in 1 2 3 4 5; do
              STATUS=$(curl -s -o /dev/null -w "%{http_code}" http://localhost:8000/health || echo "000")
              if [ "$STATUS" = "200" ]; then
                echo "Health check OK (tentativa $i)"
                break
              fi
              echo "Health check falhou ($STATUS), aguardando... ($i/5)"
              sleep 5
              if [ $i -eq 5 ]; then
                echo "ERRO: Backend não respondeu após deploy. Verifique os logs."
                docker compose -f docker-compose.prod.yml logs --tail=50 backend
                exit 1
              fi
            done

            echo "==> Status final"
            docker compose -f docker-compose.prod.yml ps
